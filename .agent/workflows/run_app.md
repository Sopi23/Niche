---
description: Run the Niche Learning Platform locally
---

To run the application locally, use the Vite development server command:

```bash
npm run dev
```

This will start the server, usually at `http://localhost:5173`.

To build for production:

```bash
npm run build
```
